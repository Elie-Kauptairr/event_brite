<div class="container d-flex justify-content-center align-items-center vh-100">
  <div class="text-center bg-dark text-white p-5 rounded w-75">
    <h1 class="display-4">Bienvenue sur Eventbrite-clone</h1>
    <p class="lead">Découvrez et participez aux événements à venir, ou créez le vôtre dès aujourd'hui !</p>
    <hr class="my-4">
    <p>Envie de partager votre propre événement avec le monde ?</p>
    <%= link_to 'Créer un événement', new_event_path, class: 'btn btn-primary btn-lg' %>
  </div>
</div>

<div class="container mt-5">
  <h2 class="text-center my-4">Événements à venir</h2>

  <% if @events.any? %>
    <div class="row justify-content-center">
      <% @events.each do |event| %>
        <div class="col-md-4 mb-4">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title"><%= event.title %></h5>
              <p class="card-text"><%= truncate(event.description, length: 100) %></p>
              <p>Date : <%= l(event.start_date, format: :long) %></p>
              <p>Lieu : <%= event.location %></p>
              <%= link_to 'Voir cet événement', event_path(event), class: 'btn btn-primary' %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-center">Aucun événement disponible pour le moment. Soyez le premier à en créer un !</p>
  <% end %>
</div>
